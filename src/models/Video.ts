export default interface Video {
    channelId: string,
    channelThumbnail?: string,
    channelTitle: string,
    comments?: Array<any>,
    pos?: number,
    videoId: string,
    videoThumbnail: string,
    videoTitle: string,
}