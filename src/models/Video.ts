export default interface Video {
    channelId: string,
    channelThumbnail?: string,
    channelTitle: string,
    comments?: Array < any >,
    videoId: string,
    videoThumbnail: string,
    videoTitle: string,
}